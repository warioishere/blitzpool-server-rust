[package]
name = "bp-mining-job"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
publish.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
bp-common = { workspace = true }
bp-share = { workspace = true }
tracing = { workspace = true }
bitcoin = { workspace = true }
thiserror = { workspace = true }
hex = { workspace = true }

[dev-dependencies]
bp-test-support = { workspace = true }
proptest = { workspace = true }
tokio = { workspace = true }
bp-regtest-harness = { workspace = true }
bp-template-distribution = { workspace = true }
# The rotating-payout regtest gate (`tests/regtest_rotating_payout.rs`) needs a
# real rotating identity to build a coinbase from, and `bp-payout-descriptor` is
# the only crate that can mint one. Dev-only: this crate must NOT take
# `miniscript` into the built artifact — the trait-object split in `bp-common`
# exists precisely so it does not have to.
bp-payout-descriptor = { workspace = true }
# `deriveaddresses` params for the independent cross-check against Core.
serde_json = { workspace = true }
